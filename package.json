{
  "name": "finsense-python-backend",
  "version": "2.0.0",
  "description": "AI-powered expense tracking backend - Python FastAPI version",
  "main": "main.py",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port 3000",
    "dev": "uvicorn main:app --host 0.0.0.0 --port 3000 --reload",
    "test": "python test_app.py",
    "install": "pip install -r requirements.txt",
    "build-frontend": "cd frontend && npm install && npm run build",
    "setup": "npm run install && npm run build-frontend"
  },
  "keywords": [
    "expense",
    "tracking",
    "ai",
    "receipt",
    "finance",
    "python",
    "fastapi"
  ],
  "author": "Finsense Team",
  "license": "MIT",
  "engines": {
    "python": ">=3.11.0"
  },
  "dependencies": {
    "tesseract.js": "^4.1.1"
  }
}
